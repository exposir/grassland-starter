{
  "name": "issue-vite",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "vitepress": "^0.22.3"
  },
  "scripts": {
    "start": "node index.js && vitepress dev docs",
    "build": "node index.js && vitepress build docs",
    "serve": "node index.js && vitepress serve docs"
  },
  "dependencies": {
    "grassland-core": "^0.0.4"
  }
}